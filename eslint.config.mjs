import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
    // Rust, not JS.
    "target/**",
  ]),
  {
    rules: {
      // A leading underscore is the conventional marker for a parameter that
      // exists to satisfy an interface but isn't used yet. `SolanaChainAdapter`
      // is full of them on purpose: the signatures are real and typed against
      // `ChainAdapter`, the bodies are waiting on a deployed program.
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
        },
      ],
    },
  },
]);

export default eslintConfig;
